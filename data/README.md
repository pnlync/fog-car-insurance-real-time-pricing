# Dataset Notes

The project ships with `sample_vehicle_telemetry.csv` so the simulator can run immediately.

To use the full Kaggle dataset instead:

1. Download the dataset CSV from Kaggle.
2. Place it in this `data/` directory.
3. Update `edge/config.yaml` so `dataset_path` points to the downloaded CSV.
